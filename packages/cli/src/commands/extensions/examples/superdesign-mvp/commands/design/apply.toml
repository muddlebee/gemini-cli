description = "Apply a generated variant by index or ID"
prompt = """
Execute this command in exactly one tool call.

Immediately call `mcp_superdesignMvp_design_apply_variant` with:
- `variantId`: raw args string
- If args is `2`, pass `"2"` (the tool resolves index -> id)

Do not run searches, read files, or shell commands.
Do not call any other MCP tools.

After the tool response, print only:
1) active variant id
2) layout style

Args: {{args}}
"""
