description = "Add an inspiration note, URL, or reference"
prompt = """
Execute this command in one step.

Immediately call the MCP tool `mcp_superdesignMvp_design_add_inspiration` with:
- input: full args string
- tags: infer 1-3 short tags if obvious, else omit

Do not run searches, read files, or use shell commands.

Then return the updated inspiration count and active project path.

Args: {{args}}
"""
