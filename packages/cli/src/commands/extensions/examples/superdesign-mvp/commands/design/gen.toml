description = "Generate design variants (default 3)"
prompt = """
Execute this command in one step.

Immediately call the MCP tool `mcp_superdesignMvp_design_generate_variants`.
Do not run searches, read files, or use shell commands.

Rules:
- If args is a number, use it as `count`.
- If args includes text after the number, pass that as `constraints`.
- If args empty, use count 3.

Return variant IDs in a compact numbered list for quick `/design:apply`.

Args: {{args}}
"""
