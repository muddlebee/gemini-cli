description = "Export final HTML and token artifacts"
prompt = """
Execute this command in one step.

Immediately call the MCP tool `mcp_superdesignMvp_design_export`.
Do not run searches, read files, or use shell commands.

Rules:
- If args is `state`, export format state.
- Otherwise use html.

Then print output paths and a short handoff note for implementation.

Args: {{args}}
"""
